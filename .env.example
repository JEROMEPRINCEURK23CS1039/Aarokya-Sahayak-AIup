# Root environment example for local development (copy this to .env or server/.env)
# NOTE: The server already contains a server/.env.example — use that as the authoritative template for backend.

# MongoDB connection (use Atlas URI for production)
MONGO_URI=mongodb://localhost:27017/aarogya-sahayak

# JWT secret (use a long random value in production)
JWT_SECRET=replace_with_a_strong_secret

# ML service URL (local dev default)
ML_SERVICE_URL=http://localhost:5001/api/ml/predict

# Frontend client URL (for CORS / OAuth callbacks)
CLIENT_URL=http://localhost:5173
